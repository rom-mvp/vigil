{
  "name": "vigil",
  "version": "1.0.0",
  "description": "Pre-LLM Security Gateway",
  "scripts": {
    "start": "./start.sh",
    "stop": "pkill -f 'python.*local_server.py|python.*mock_agentshield.py' || true",
    "logs": "tail -f /tmp/agentshield.log /tmp/gateway.log",
    "test": "PYTHONPATH=src pytest tests/",
    "test:unit": "PYTHONPATH=src pytest tests/unit/ -v",
    "test:integration": "PYTHONPATH=src pytest tests/integration/ -v",
    "test:performance": "PYTHONPATH=src pytest tests/performance/ -v",
    "test:coverage": "PYTHONPATH=src pytest --cov=src/vigil --cov-report=html --cov-report=term",
    "health": "curl -s http://localhost:9000/health && curl -s http://localhost:8000/health && echo '\\n✅ All services healthy'"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/rom-mvp/vigil"
  },
  "keywords": [
    "security",
    "llm",
    "gateway",
    "agentshield"
  ],
  "author": "rom-mvp",
  "license": "MIT"
}
